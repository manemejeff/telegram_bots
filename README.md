# Hub for my telegram bots
### simple_bot
- config.py with [token] and [url(if required)] constants needs to be added

### weather_bot
- config.py with [token] and [url(if required)] constants needs to be added
- openweather api key
- lang id